app = 'kinowo'
primary_region = 'waw'

[build]
  dockerfile = 'Dockerfile'

[env]
  PORT = '9000'

[http_service]
  internal_port = 9000
  force_https = true
  # Keep at least one machine running so users never pay a cold-start hit;
  # auto_stop disabled so Fly doesn't try to scale to zero on idle.
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = 'requests'
    hard_limit = 50
    soft_limit = 25

[[vm]]
  # 768mb on Temurin 26. With the shared-MongoClient + JVM-caps fix
  # in place, HotSpot RSS peaks around ~400 MB plus kernel/runtime
  # overhead; 768mb gives ~250 MB headroom. Bump to 1gb if the next
  # class-bearing feature pushes it over again.
  memory = '768mb'
  cpu_kind = 'shared'
  cpus = 1

[checks]
  [checks.health]
    grace_period = '30s'
    interval = '15s'
    method = 'GET'
    path = '/health'
    port = 9000
    timeout = '10s'
    type = 'http'
