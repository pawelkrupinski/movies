#
# Per-country pipeline convergence.
#
# Asks, for each country independently: does the scrape → fold → settle loop
# reach a fixpoint on THIS country's cinema catalogue and country-scoped title
# rules? The corpus is written into a real `cinema_scrapes` collection and
# replayed back out, so the archive's round-trip is on the path too.
#
# One leg per country, in parallel. They must be separate JOBS, not threads in
# one JVM: each leg installs its country's `TitleRuleSet` into `TitleNormalizer`
# process-globally, so two countries sharing a process would overwrite each
# other's normalisation and silently test the wrong rules. Each leg also gets its
# own MongoDB container and — inside it — a uniquely named database per run
# (`IsolatedMongoDatabase`), so nothing here can collide with anything else,
# including a re-run of itself.
#
# Deliberately NOT part of ci.yml: that workflow runs against a hard 20-runner
# budget (1 test + 1 integration + 4 e2e + 1 mobile + 13 page-tests), and three
# more legs would have meant re-cutting the existing shards. This runs on its
# own on pushes that touch the pipeline, and nightly to catch drift that lands
# through a data or dependency change rather than a code one.
#
name: Country convergence

on:
    # Runs AFTER `Deploy to Fly.io` finishes — not on push. These legs are
    # minutes long (Poland 3, Germany 9, the UK
    # 14) and firing them in parallel with the deploy workflow put three
    # heavyweight jobs on the runner pool at exactly the moment the deploy needed
    # it, for a signal nobody gates the deploy on anyway. Chaining keeps the
    # deploy's "all green" arriving as early as it always did and still runs the
    # convergence check against the very commit that shipped.
    schedule:
        # 04:20 UTC — after the nightly country-fixture recorder, before the day starts.
        - cron: '20 4 * * *'
    workflow_dispatch:

concurrency:
    # One lane for the whole workflow, so a new run always supersedes the one
    # still in flight. Keying on the head SHA and the event name (as this did)
    # gave every deploy, nightly and manual run its own lane, which meant
    # nothing ever cancelled anything: three legs of up to 75 minutes each piled
    # up on the runner pool while the only run whose result anyone reads is the
    # newest one. All three triggers land on `main`, so the ref keeps a lane per
    # branch for the rare dispatch from somewhere else without splitting main's.
    group: country-convergence-${{ github.ref }}
    # Unconditional again, and safe now: every run of this workflow is either a
    # deploy-triggered dispatch or a deliberate one, so there are no runs that exist
    # only to skip. Previously a skipped `workflow_run` run took the lane and
    # cancelled a leg 21.5 minutes into real work; the deploy dispatching us
    # explicitly removes that class of run entirely.
    cancel-in-progress: true

jobs:
    convergence:
        name: convergence (${{ matrix.country }})
        strategy:
            # Every country reports independently — one country's flap must not
            # hide whether the other two converged.
            fail-fast: false
            matrix:
                include:
                    - { country: poland,         code: pl, cmd: convergencePoland }
                    - { country: germany,        code: de, cmd: convergenceGermany }
                    - { country: united-kingdom, code: uk, cmd: convergenceUk }
        runs-on: ubuntu-latest
        # 45 was not enough headroom and cost a real run: on 2026-07-28 the Germany
        # and UK legs were CANCELLED at exactly 45m while Poland finished in 8,
        # because the corpus was mis-sized (Poland's films-per-venue ratio applied
        # to every country — see CountryScrapeCorpus.shapeOf). The sizing is fixed,
        # but a CI runner is a good deal slower than the machine these are timed on,
        # so the ceiling now sits well clear of the expected run rather than just
        # above it. A leg that approaches this is a signal to re-measure the shape,
        # not to raise the number again.
        timeout-minutes: 75
        permissions:
            contents: read
            checks: write
            # `gh run download` needs to list runs and read artifacts of this repo.
            actions: read
        steps:
            - uses: actions/checkout@v5

            # Restore BOTH recorded fixture sets, each from its own workflow and its
            # own cache key: the corpus from the nightly `Record scrape fixtures`, the
            # enrichment answers from `Record enrichment fixtures`, which runs after
            # every convergence. Restore-only and prefix-matched, so a leg always takes
            # the newest capture of each and simply proceeds without one when none
            # exists — the spec then falls back to fetching live and persisting, which
            # is what produces the next capture.
            - name: Restore recorded scrape fixtures
              uses: actions/cache/restore@v5
              with:
                  path: scrape-archive
                  key: scrape-fixtures-${{ matrix.code }}-
                  restore-keys: scrape-fixtures-${{ matrix.code }}-

            # Enrichment fixtures come from the ARTIFACT, not the cache. They are
            # 84-152 MB per country, and this repo's actions cache already sits at
            # 9.46 GB of a 10 GB ceiling (mostly sbt-target entries), so three
            # enrichment tarballs were evicted the moment they were written — with
            # `actions/cache/save` still reporting success, because a save succeeding
            # says nothing about whether the entry SURVIVES. Worse, pushing 370 MB
            # through that ceiling evicts the sbt caches that keep every job from
            # cold-compiling. Artifacts have their own retention and cannot evict a
            # build cache.
            #
            # `continue-on-error` because a missing artifact is the normal first-run
            # state: the leg fetches live and the recorder captures it afterwards.
            # Enrichment fixtures come from the ARTIFACT of the latest successful
            # recording, not from the cache. They are 84-152 MB per country and this
            # repo's actions cache already sits at 9.46 GB of a 10 GB ceiling (mostly
            # sbt-target entries), so three enrichment tarballs were evicted the moment
            # they were written — with `actions/cache/save` still reporting success,
            # because a save succeeding says nothing about whether the entry SURVIVES.
            # Worse, forcing 370 MB through that ceiling evicts the sbt caches that keep
            # every job off a cold compile.
            #
            # Resolved with `gh` rather than `actions/download-artifact`, which can only
            # address a run id — and the id available here belongs to the DEPLOY that
            # triggered this workflow, not to the recorder. Asking for "the newest
            # successful recording" is what we actually mean.
            #
            # A missing artifact is the normal first-run state, so this never fails the
            # leg: it falls through to fetching live, and the recorder captures it after.
            - name: Download the latest recorded enrichment fixtures
              continue-on-error: true
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              run: |
                  set -uo pipefail
                  RUN=$(gh run list --workflow "Record enrichment fixtures" --status success \
                        --limit 1 --json databaseId --jq '.[0].databaseId' 2>/dev/null || true)
                  if [ -z "${RUN:-}" ]; then
                      echo "no successful enrichment recording yet — this leg fetches live"
                      exit 0
                  fi
                  echo "downloading enrichment fixtures from recording run $RUN"
                  gh run download "$RUN" --name "enrichment-fixtures-${{ matrix.code }}" \
                     --dir enrichment-archive || echo "no artifact for ${{ matrix.code }} in run $RUN"
                  ls -l enrichment-archive/ 2>/dev/null || true

            - name: Unpack whichever fixtures are present
              run: |
                  set -euo pipefail
                  for archive in "scrape-archive/scrapes-${{ matrix.code }}.tar.gz" \
                                 "enrichment-archive/enrichment-${{ matrix.code }}.tar.gz"; do
                      if [ -f "$archive" ]; then
                          tar -xzf "$archive"
                          echo "restored $archive ($(du -h "$archive" | cut -f1))"
                      else
                          echo "no $archive — this leg fetches live for it and the recorder captures it next"
                      fi
                  done
                  # `|| true` and a subshell: a MISSING fixture directory is the normal
                  # fallback path, not an error — the leg then fetches live and the
                  # recorder captures it next. Without this, `find` exiting non-zero on
                  # an absent path tripped `set -euo pipefail` and failed the whole leg
                  # AFTER printing the count, which is how three legs died 40 seconds in
                  # with both restores reporting success.
                  available=$( { find test/resources/fixtures/corpus \
                                      "test/resources/fixtures/enrichment-${{ matrix.code }}" \
                                      -type f 2>/dev/null || true; } | wc -l )
                  echo "fixture files available: $available"

            - uses: actions/setup-java@v5
              with:
                  distribution: temurin
                  java-version: 25
                  cache: sbt
            - uses: sbt/setup-sbt@v1

            - uses: actions/cache@v5
              with:
                  path: |
                      target
                      project/target
                      project/project/target
                  key: sbt-target-convergence-${{ runner.os }}-${{ hashFiles('build.sbt', 'project/build.properties', 'project/plugins.sbt', '**/src/main/**/*.scala', '**/src/test/**/*.scala', '**/src/fixtures/**/*.scala') }}
                  restore-keys: |
                      sbt-target-convergence-${{ runner.os }}-
                      sbt-target-e2e-${{ runner.os }}-
                      sbt-target-${{ runner.os }}-

            - run: sbt ${{ matrix.cmd }}
              env:
                  # The spec installs this country's TitleRuleSet itself; naming the
                  # country here too means anything that reads the environment
                  # directly (`Country.soleFromEnv`) agrees with it rather than
                  # falling back to the Poland default.
                  KINOWO_COUNTRY:   ${{ matrix.code }}
                  KINOWO_COUNTRIES: ${{ matrix.code }}
                  # Replay this country's REAL repertoire instead of the generated
                  # corpus. The generated one cannot exercise enrichment at all —
                  # every row carries `director = Seq("Some Director")`, which sends
                  # resolution down the director-walk branch and guarantees a miss —
                  # so without this the enrichment fields sit the convergence claim
                  # out as None. `_DB` defaults to the country's own database.
                  KINOWO_CONVERGENCE_SCRAPES_URI: ${{ secrets.CONVERGENCE_MONGODB_URI }}
                  # The enrichment cache, on the shared cluster rather than in this
                  # leg's throwaway container: a container-local cache would be cold
                  # every run, which is a full live sweep against TMDB/IMDb/Filmweb/
                  # RT/Metacritic each time. Shared, the first run fills it and the
                  # rest replay it until the 1-day TTL turns it over.
                  KINOWO_CONVERGENCE_CACHE_URI:   ${{ secrets.CONVERGENCE_MONGODB_URI }}
                  # Without a real key TmdbClient short-circuits and nothing resolves,
                  # so the cache would have nothing to hold and the real corpus would
                  # buy nothing over the generated one.
                  # Replay recorded enrichment answers first; anything new falls through
                  # to the cache/live chain and is recorded for the next run.
                  KINOWO_CONVERGENCE_ENRICHMENT_FIXTURES: enrichment-${{ matrix.code }}
                  TMDB_API_KEY:     ${{ secrets.TMDB_API_KEY }}

            - name: Publish convergence test report
              if: always()
              uses: mikepenz/action-junit-report@v5
              with:
                  report_paths: 'target/test-reports/unit/**/*.xml'
                  check_name: convergence (${{ matrix.country }})
                  detailed_summary: true
                  include_passed: false

            # A leaked database means the drop didn't run — worth seeing in the log
            # rather than discovering as a slowly filling disk.
            - name: Report any leaked isolated databases
              if: always()
              run: |
                  docker exec mongo mongosh --quiet --eval \
                    'db.adminCommand({listDatabases:1,nameOnly:true}).databases
                       .map(d => d.name).filter(n => n.startsWith("kinowo_isolated")).join("\n")'

    # Kick the enrichment recorder off explicitly, for the same reason the deploy now
    # kicks US off explicitly rather than by `workflow_run`.
    #
    # Two problems with chaining here. GitHub suppresses some event cascades from runs
    # started by `GITHUB_TOKEN`, and this workflow is now token-dispatched by the
    # deploy — so a `workflow_run` chain out of it is exactly the kind of link that
    # silently never fires. And `workflow_run` cannot filter on conclusion, so gating
    # inside the recorder would create runs that exist only to skip, which is what
    # cancelled a leg 21.5 minutes into real work.
    #
    # `always()`: the recorder must run whether the legs passed or failed. A FAILED leg
    # has usually still enriched most of the corpus and written it through to the
    # shared cache, so gating on success would discard the capture precisely on the
    # runs that populated it most.
    record-enrichment:
        needs: convergence
        if: always()
        runs-on: ubuntu-latest
        permissions:
            actions: write
        steps:
            - name: Start the enrichment fixture recorder
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  GH_REPO: ${{ github.repository }}
              run: |
                  set -euo pipefail
                  echo "convergence finished (${{ needs.convergence.result }}) — dispatching the enrichment recorder"
                  gh workflow run "Record enrichment fixtures" --ref "${GITHUB_REF_NAME}"
