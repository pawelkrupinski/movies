package controllers

/** The address the legal pages tell people to write to. One constant rather than
 *  the same mailto repeated across four templates in three languages — a change
 *  of address must not be able to land in two of them and miss the rest. */
object LegalContact {
  val Email: String = "pawel.krupinski@gmail.com"
}
