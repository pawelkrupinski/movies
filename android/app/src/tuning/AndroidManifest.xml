<?xml version="1.0" encoding="utf-8"?>
<!--
  Tuning overlay. Adds a second launcher icon ("Kinowo Tune") that opens the
  non-prod tweak screen on-device without adb (see TuningLauncherActivity).
  Merged in only for the tuning-enabled variants (debug + tuneRelease) via the
  shared src/tuning source set, so it never ships in the public release build.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".TuningLauncherActivity"
            android:exported="true"
            android:label="Kinowo Tune"
            android:taskAffinity="pl.kinowo.tune">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
