{
  "ci": {
    "collect": {
      "url": ["https://kinowo.fly.dev/"],
      "numberOfRuns": 3,
      "settings": {
        "preset": "desktop"
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    },
    "assert": {
      "_comment": [
        "Perf stays 'warn' because mobile-throttled scores swing by 0.1–0.2",
        "between runs on the free Fly tier — a hard gate would flake. The",
        "remaining three categories are stable run-to-run so they're 'error',",
        "i.e. a regression fails the post-deploy lighthouse workflow and",
        "wakes Claude via the on-failure action wired in lighthouse.yml."
      ],
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.6 }],
        "categories:accessibility": ["error", { "minScore": 0.7 }],
        "categories:best-practices": ["error", { "minScore": 0.7 }],
        "categories:seo": ["error", { "minScore": 0.8 }]
      }
    }
  }
}
